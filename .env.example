# Backend API origin (no trailing slash).
# - Local dev (MSW): leave empty → requests go to same-origin `/api/v1`
# - Vercel Preview/Production: set to deployed backend URL
#   e.g. https://port-0-ject-test-mordxkhh8215fb40.sel3.cloudtype.app
NEXT_PUBLIC_API_ORIGIN=
